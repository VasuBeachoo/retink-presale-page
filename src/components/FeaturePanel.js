import styled from "styled-components";

export const FeaturePanelBox = styled.div``;

const FeaturePanel = ({ className }) => {
  return <FeaturePanelBox className={className}></FeaturePanelBox>;
};

export default FeaturePanel;
