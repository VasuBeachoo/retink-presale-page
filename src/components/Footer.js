import styled from "styled-components";

export const FooterBox = styled.footer``;

const Footer = ({ className }) => {
  return <FooterBox className={className}></FooterBox>;
};

export default Footer;
