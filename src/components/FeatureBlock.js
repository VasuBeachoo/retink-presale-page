import styled from "styled-components";

export const FeatureBlockBox = styled.div``;

const FeatureBlock = ({ className }) => {
  return <FeatureBlockBox className={className}></FeatureBlockBox>;
};

export default FeatureBlock;
