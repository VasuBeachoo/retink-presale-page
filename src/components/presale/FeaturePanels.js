import styled from "styled-components";

export const FeaturePanelsBox = styled.section``;

const FeaturePanels = ({ className }) => {
  return <FeaturePanelsBox className={className}></FeaturePanelsBox>;
};

export default FeaturePanels;
